---
import Icon from './Icon.astro';
import iconLight from '../assets/Icons/Add Icon Light.png';
import iconDark from '../assets/Icons/Add Icon Dark.png';

interface Props {
  collection: string;
}

const { collection } = Astro.props;
const defaultBranch = import.meta.env.PUBLIC_DEFAULT_BRANCH || 'main';
const branchPrefix = import.meta.env.PUBLIC_KEYSTATIC_MODE === 'github' ? `/branch/${defaultBranch}` : '';
const href = `/keystatic${branchPrefix}/collection/${collection}/create`;
---

<a href={href} class="keystatic-link" title="Add new entry">
  <button type="button" class="keystatic-btn">
    <Icon src={iconLight} darkSrc={iconDark} size={14} />
    Add
  </button>
</a>

<style>
  .keystatic-link {
    text-decoration: none;
  }

  .keystatic-btn {
    min-height: 23px;
    padding: 2px 8px;
    font-size: 12px;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 4px;
  }
</style>
